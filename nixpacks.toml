[phases.setup]
nixPkgs = ["nodejs_20"]
aptPkgs = []
cmds = ["npm install -g pnpm@10"]

[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @tms/backend build"]

[start]
cmd = "cd apps/backend && node dist/index.js"
