# API Configuration
VITE_API_URL=http://localhost:4000

# Auth Provider ('self-hosted' or 'auth0')
VITE_AUTH_PROVIDER=self-hosted

# SAML Configuration
VITE_SAML_ENABLED=false  # Set to 'true' when backend SAML is enabled

# Auth0 Configuration (only needed if VITE_AUTH_PROVIDER=auth0)
# VITE_AUTH0_DOMAIN=your-tenant.auth0.com
# VITE_AUTH0_CLIENT_ID=your-client-id
# VITE_AUTH0_AUDIENCE=https://api.tms.company.com
